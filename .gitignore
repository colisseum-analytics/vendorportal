node_modules
dist
.env.local
.env
.DS_Store
.claude
*.csv

# Full-platform backups contain user PII (emails, contact messages) —
# never commit these, download them from the backup workflow instead.
backup/
