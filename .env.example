# Copy this file to .env.local for local development.
# In Vercel/Netlify, add these as Environment Variables in project settings instead —
# never commit your real .env.local to git (it's already in .gitignore).

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
